<div class="container">
    <div class="row">
        <div class="col-lg-12">
            {{#if controller.newLog}}
                {{partial "login/partials/-new"}}
            {{else}}
                {{partial "login/partials/-submit"}}
            {{/if}}
        </div>
    </div>
</div>
