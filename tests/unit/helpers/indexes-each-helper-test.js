import {
  indexesEachHelper
} from 'seaoracle/helpers/indexes-each-helper';

module('IndexesEachHelperHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = indexesEachHelper(42);
  ok(result);
});
