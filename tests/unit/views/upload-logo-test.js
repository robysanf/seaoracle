import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:upload-logo', 'UploadLogoView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
